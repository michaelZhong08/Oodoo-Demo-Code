<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="turorial_theme.list">
      <field name="name">Turorial theme list</field>
      <field name="model">turorial_theme.turorial_theme</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="turorial_theme.action_window">
      <field name="name">Turorial theme window</field>
      <field name="res_model">turorial_theme.turorial_theme</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="turorial_theme.action_server">
      <field name="name">Turorial theme server</field>
      <field name="model_id" ref="model_turorial_theme_turorial_theme"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Turorial theme" id="turorial_theme.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="turorial_theme.menu_1" parent="turorial_theme.menu_root"/>
    <menuitem name="Menu 2" id="turorial_theme.menu_2" parent="turorial_theme.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="turorial_theme.menu_1_list" parent="turorial_theme.menu_1"
              action="turorial_theme.action_window"/>
    <menuitem name="Server to list" id="turorial_theme" parent="turorial_theme.menu_2"
              action="turorial_theme.action_server"/>
    -->
  </data>
</odoo>